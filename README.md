# mini-ai-from-scratch
A beginner-friendly Python project that shows how a simple linear regression model learns using loss, gradients, and weight updates.

# Linear Regression From Scratch

This project demonstrates the core training loop of a simple machine learning model without using any machine learning library.

The model learns a linear relationship:

```python
y_pred = w * x + b
